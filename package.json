{
  "name": "@sisa/react-configs",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "release:version": "changeset version -r",
    "release:publish": "changeset publish -r",
    "pre-release:version": "changeset version --snapshot next",
    "pre-release:publish": "changeset publish --tag next",
    "pre-release:enter": "changeset pre enter next",
    "pre-release:exit": "changeset pre exit next",
    "check": "biome check",
    "format": "biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.4",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@sisa/biome-configs": "^0.0.5",
    "lefthook": "^1.11.13",
    "turbo": "^2.5.3"
  }
}
